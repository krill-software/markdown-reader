name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  release:
    uses: krill-software/.github/.github/workflows/krill-app-release.yml@main
    with:
      product-name: "Markdown Viewer"
      example-file: "notes.md"
    secrets: inherit
    permissions:
      contents: write
